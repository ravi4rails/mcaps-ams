<%= form_for(@emaployee_category) do |f| %>
  <% if @emaployee_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@emaployee_category.errors.count, "error") %> prohibited this emaployee_category from being saved:</h2>

      <ul>
      <% @emaployee_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
