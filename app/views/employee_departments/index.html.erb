<p id="notice"><%= notice %></p>

<h1>Listing Employee Departments</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Dept code</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @employee_departments.each do |employee_department| %>
      <tr>
        <td><%= employee_department.name %></td>
        <td><%= employee_department.dept_code %></td>
        <td><%= link_to 'Show', employee_department %></td>
        <td><%= link_to 'Edit', edit_employee_department_path(employee_department) %></td>
        <td><%= link_to 'Destroy', employee_department, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Employee department', new_employee_department_path %>
